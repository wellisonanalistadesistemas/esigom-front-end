import { SituacaoRemessaAtualPipe } from './situacao-remessa-atual.pipe';

describe('SituacaoRemessaAtualPipe', () => {
  it('create an instance', () => {
    const pipe = new SituacaoRemessaAtualPipe();
    expect(pipe).toBeTruthy();
  });
});
