import { SiglaUnidadeGestoraPipe } from './sigla-unidade-gestora.pipe';

describe('SiglaUnidadeGestoraPipe', () => {
  it('create an instance', () => {
    const pipe = new SiglaUnidadeGestoraPipe();
    expect(pipe).toBeTruthy();
  });
});
