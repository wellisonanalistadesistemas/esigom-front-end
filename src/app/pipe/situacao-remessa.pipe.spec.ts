import { SituacaoRemessaPipe } from './situacao-remessa.pipe';

describe('SituacaoRemessaPipe', () => {
  it('create an instance', () => {
    const pipe = new SituacaoRemessaPipe();
    expect(pipe).toBeTruthy();
  });
});
