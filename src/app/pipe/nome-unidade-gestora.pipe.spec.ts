import { NomeUnidadeGestoraPipe } from './nome-unidade-gestora.pipe';

describe('NomeUnidadeGestoraPipe', () => {
  it('create an instance', () => {
    const pipe = new NomeUnidadeGestoraPipe();
    expect(pipe).toBeTruthy();
  });
});
