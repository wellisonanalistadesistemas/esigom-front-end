import { TipoRecursoOrcamentarioPipe } from './tipo-recurso-orcamentario.pipe';

describe('TipoRecursoOrcamentarioPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoRecursoOrcamentarioPipe();
    expect(pipe).toBeTruthy();
  });
});
